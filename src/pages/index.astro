---
import TemplateLayout from "../layouts/TemplateLayout.astro";
import TopBanner from "../components/TopBanner.astro";
---

<TemplateLayout title="首页">
  <!-- ===== 模板风格：首屏（放在 banner 之后的内容区）===== -->
  <section class="home-hero">
    <div class="row">
      <div class="col-md-7 col-sm-12">
        <div class="hero-copy">
          <div class="hero-kicker">MELBOURNE · CENDANT</div>
          <h2 class="hero-title">墨尔本 · 胜腾国际</h2>
          <p class="hero-desc">
            专注澳洲房产投资、项目开发管理、物业租赁与移民留学咨询等一站式服务。
          </p>

          <div class="hero-actions">
            <a class="btn btn-primary hero-btn" href="/contact">立即咨询</a>
            <a class="btn btn-default hero-btn ghost" href="#services">查看服务项目</a>
          </div>

          <div class="hero-badges">
            <span class="badge-pill">本地资源整合</span>
            <span class="badge-pill">交易闭环服务</span>
            <span class="badge-pill">合规与风控</span>
          </div>
        </div>
      </div>

      <div class="col-md-5 col-sm-12">
        <!-- 模板感“右侧信息卡” -->
        <div class="hero-panel">
          <div class="panel-title">快速入口</div>
          <a class="panel-link" href="/real-estate">地产资讯</a>
          <a class="panel-link" href="/immigration">移民服务</a>
          <a class="panel-link" href="/company-profile">公司概况</a>
          <a class="panel-link" href="/contact">联系我们</a>

          <div class="panel-split"></div>

          <div class="panel-meta">
            <div><b>电话：</b>+61 420 616 883</div>
            <div><b>邮箱：</b>info@cendantpgau.com</div>
            <div><b>地址：</b>Docklands VIC 3008</div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- ===== 服务项目（你原本首页的内容）===== -->
  <section id="services" class="home-section">
    <TopBanner zh="服务项目" en="SERVICE AVAILABLE" />

    <div class="row cards">
      <div class="col-md-4 col-sm-6 col-xs-12">
        <a class="service-card" href="/real-estate">
          <h3>房地产买卖</h3>
          <p>项目推荐、选房策略、谈判签约、交割协助。</p>
          <span class="more">查看详情 →</span>
        </a>
      </div>

      <div class="col-md-4 col-sm-6 col-xs-12">
        <a class="service-card" href="/real-estate">
          <h3>房地产开发</h3>
          <p>项目评估、开发管理、资源对接、风控与交付。</p>
          <span class="more">查看详情 →</span>
        </a>
      </div>

      <div class="col-md-4 col-sm-6 col-xs-12">
        <a class="service-card" href="/real-estate">
          <h3>物业租赁</h3>
          <p>招租、租客筛选、租约管理、长期维护服务。</p>
          <span class="more">查看详情 →</span>
        </a>
      </div>

      <div class="col-md-4 col-sm-6 col-xs-12">
        <a class="service-card" href="/immigration">
          <h3>移民服务</h3>
          <p>雇主担保、商业投资类路径评估与材料支持。</p>
          <span class="more">查看详情 →</span>
        </a>
      </div>

      <div class="col-md-4 col-sm-6 col-xs-12">
        <a class="service-card" href="/contact">
          <h3>联系我们</h3>
          <p>预约咨询、合作洽谈、项目对接与资料获取。</p>
          <span class="more">立即联系 →</span>
        </a>
      </div>
    </div>
  </section>

  <!-- ===== 公司简介（更商业化段落）===== -->
  <section class="home-section">
    <TopBanner zh="公司概况" en="COMPANY PROFILE" />

    <div class="row profile">
      <div class="col-md-7 col-sm-12">
        <div class="profile-box">
          <h3 class="profile-title">高端投资服务与本地资源整合</h3>
          <p>
            我们以“稳健、合规、可落地”的投资逻辑为核心，面向澳洲本地与海外客户，提供从项目评估、交易执行到资产管理的一体化服务。
            同时为家庭与企业客户提供配套的移民与留学咨询支持，确保整体方案可执行、可交付。
          </p>
          <ul>
            <li>本地项目资源：开发商 / 代理 / 律师 / 会计 / 贷款经纪协作</li>
            <li>交易闭环：尽调、谈判、合同、交割、税务与持有策略</li>
            <li>资产管理：租赁管理、维护协调、收益优化与长期规划</li>
          </ul>
        </div>
      </div>

      <div class="col-md-5 col-sm-12">
        <div class="profile-side">
          <div class="side-title">为什么选择我们</div>

          <div class="side-item">
            <div class="dot"></div>
            <div>
              <b>本地团队</b>
              <p>深耕墨尔本市场，信息透明，沟通高效。</p>
            </div>
          </div>

          <div class="side-item">
            <div class="dot"></div>
            <div>
              <b>专业合规</b>
              <p>强调风控与合规流程，减少交易不确定性。</p>
            </div>
          </div>

          <div class="side-item">
            <div class="dot"></div>
            <div>
              <b>长期服务</b>
              <p>从买入到持有、出租、维护与优化，持续支持。</p>
            </div>
          </div>

          <a class="cta-link" href="/contact">获取专属方案 →</a>
        </div>
      </div>
    </div>
  </section>

  <!-- ===== 底部 CTA（模板风格）===== -->
  <section class="home-cta">
    <div class="cta-inner">
      <div>
        <div class="cta-title">准备开始您的项目咨询？</div>
        <div class="cta-desc">我们会在工作时间内尽快回复，提供清晰的下一步建议。</div>
      </div>
      <a class="btn btn-primary cta-btn" href="/contact">去填写表单</a>
    </div>
  </section>

  <style>
    /* 深蓝品牌：和你现在网站一致 */
    :global(:root){
      --brand:#091a5d;
      --brand2:#0b1f6b;
      --border:#e2e6f0;
      --text:#0e1a2b;
    }

    /* 模板通常已有 container/row/col，这里只补“商业感”细节 */
    .home-hero{
      padding: 10px 0 30px;
    }
    .hero-copy{
      padding: 10px 6px;
    }
    .hero-kicker{
      letter-spacing: 2px;
      font-size: 12px;
      font-weight: 800;
      color: var(--brand);
      opacity: .7;
      margin-bottom: 8px;
    }
    .hero-title{
      margin: 0 0 12px;
      font-weight: 900;
      color: var(--brand);
      letter-spacing: .4px;
    }
    .hero-desc{
      line-height: 1.9;
      color: var(--text);
      opacity: .88;
      margin: 0 0 16px;
      font-size: 15px;
    }
    .hero-actions{
      display:flex;
      gap: 10px;
      flex-wrap: wrap;
      margin-bottom: 14px;
    }
    .hero-btn{
      border-radius: 10px;
      font-weight: 800;
    }
    .hero-btn.ghost{
      background: #fff;
      border: 1px solid var(--border);
      color: var(--brand);
    }
    .hero-badges{
      display:flex;
      gap: 8px;
      flex-wrap: wrap;
      margin-top: 6px;
    }
    .badge-pill{
      border: 1px solid var(--border);
      border-radius: 999px;
      padding: 6px 10px;
      font-size: 12px;
      font-weight: 800;
      color: var(--brand);
      background: #fff;
      opacity: .95;
    }

    .hero-panel{
      border: 1px solid var(--border);
      border-radius: 14px;
      background: #fff;
      padding: 16px;
      box-shadow: 0 18px 60px rgba(10,20,60,.08);
      margin-top: 10px;
    }
    .panel-title{
      font-weight: 900;
      color: var(--brand);
      margin-bottom: 10px;
      letter-spacing: .3px;
    }
    .panel-link{
      display:block;
      padding: 10px 12px;
      border-radius: 10px;
      border: 1px solid var(--border);
      margin-bottom: 10px;
      color: var(--brand);
      font-weight: 900;
      background: #fff;
    }
    .panel-link:hover{
      opacity:.9;
    }
    .panel-split{
      height: 1px;
      background: var(--border);
      margin: 12px 0;
    }
    .panel-meta{
      font-size: 13px;
      line-height: 1.9;
      color: var(--text);
      opacity: .85;
    }

    .home-section{
      padding: 24px 0 10px;
    }

    .cards{
      margin-top: 10px;
    }
    .service-card{
      display:block;
      border: 1px solid var(--border);
      border-radius: 14px;
      padding: 16px;
      background: #fff;
      margin-bottom: 14px;
      transition: transform .15s ease, box-shadow .15s ease;
    }
    .service-card:hover{
      transform: translateY(-2px);
      box-shadow: 0 18px 60px rgba(10,20,60,.08);
    }
    .service-card h3{
      margin: 0 0 10px;
      color: var(--brand);
      font-weight: 900;
      font-size: 18px;
    }
    .service-card p{
      margin: 0 0 10px;
      line-height: 1.8;
      color: var(--text);
      opacity: .85;
      font-size: 14px;
    }
    .service-card .more{
      font-weight: 900;
      color: var(--brand);
      font-size: 13px;
    }

    .profile-box{
      border: 1px solid var(--border);
      border-radius: 14px;
      background: #fff;
      padding: 16px;
    }
    .profile-title{
      margin: 0 0 10px;
      color: var(--brand);
      font-weight: 900;
      letter-spacing: .3px;
    }
    .profile-box p{
      line-height: 1.9;
      color: var(--text);
      opacity: .88;
    }
    .profile-box ul{
      margin: 10px 0 0 18px;
      line-height: 1.9;
      color: var(--text);
      opacity: .88;
    }

    .profile-side{
      border: 1px solid var(--border);
      border-radius: 14px;
      background: #fff;
      padding: 16px;
    }
    .side-title{
      font-weight: 900;
      color: var(--brand);
      margin-bottom: 12px;
    }
    .side-item{
      display:flex;
      gap: 10px;
      align-items:flex-start;
      margin-bottom: 12px;
    }
    .dot{
      width: 10px;
      height: 10px;
      border-radius: 999px;
      background: var(--brand2);
      margin-top: 6px;
      flex: 0 0 auto;
    }
    .side-item p{
      margin: 4px 0 0;
      color: var(--text);
      opacity: .85;
      line-height: 1.8;
      font-size: 13px;
    }
    .cta-link{
      display:inline-block;
      margin-top: 6px;
      font-weight: 900;
      color: var(--brand);
    }

    .home-cta{
      padding: 26px 0 70px;
    }
    .cta-inner{
      border: 1px solid var(--border);
      border-radius: 14px;
      background: #fff;
      padding: 16px;
      display:flex;
      align-items:center;
      justify-content:space-between;
      gap: 12px;
    }
    .cta-title{
      font-weight: 900;
      color: var(--brand);
      margin-bottom: 6px;
    }
    .cta-desc{
      color: var(--text);
      opacity: .82;
      line-height: 1.8;
      font-size: 13px;
    }
    .cta-btn{
      border-radius: 10px;
      font-weight: 900;
      white-space: nowrap;
    }

    @media (max-width: 900px){
      .cta-inner{ flex-direction: column; align-items: flex-start; }
    }
  </style>
</TemplateLayout>
---
import TemplateLayout from "../../layouts/TemplateLayout.astro";
import { getRealEstatePosts } from "../../lib/contentful";

const posts = await getRealEstatePosts(30);

// 售卖分类（你已经创建了这些文件）
const saleCats = [
  { href: "/real-estate/sale/apartment", label: "公寓" },
  { href: "/real-estate/sale/second-hand", label: "二手房" },
  { href: "/real-estate/sale/townhouse", label: "联排别墅" },
  { href: "/real-estate/sale/land-villa", label: "土地别墅" },
  { href: "/real-estate/sale/commercial", label: "商业办公楼" },
  { href: "/real-estate/sale/land", label: "土地开发" },
  { href: "/real-estate/sale/farm", label: "农场" },
  { href: "/real-estate/sale/hotel", label: "酒店" },
];

// 出租分类（你已经创建了这些文件）
const rentCats = [
  { href: "/real-estate/rent/apartment", label: "公寓" },
  { href: "/real-estate/rent/townhouse", label: "联排别墅" },
  { href: "/real-estate/rent/land-villa", label: "土地别墅" },
  { href: "/real-estate/rent/commercial", label: "商业办公" },
];
---

<TemplateLayout title="地产资讯" subtitle="REAL ESTATE">
  <section class="re-block">
    <h3 class="re-h3">房产售卖资讯</h3>
    <div class="re-choice-list">
      {saleCats.map((c) => (
        <a class="re-choice" href={c.href}>{c.label}</a>
      ))}
    </div>
  </section>

  <section class="re-block">
    <h3 class="re-h3">房产出租资讯</h3>
    <div class="re-choice-list">
      {rentCats.map((c) => (
        <a class="re-choice" href={c.href}>{c.label}</a>
      ))}
    </div>
  </section>

  <section class="re-block">
    <div class="re-head-row">
      <h3 class="re-h3" style="margin:0;">最新发布</h3>
      <a class="re-more" href="/real-estate">刷新列表</a>
    </div>

    {posts && posts.length ? (
      <div class="re-post-list">
        {posts.map((p) => (
          <a class="re-post-card" href={`/real-estate/${p.slug}`}>
            <div class="re-post-top">
              <div class="re-tag">{p.category ?? "资讯"}</div>
              <div class="re-date">
                {p.publishDate ? new Date(p.publishDate).toLocaleDateString("zh-CN") : ""}
              </div>
            </div>
            <div class="re-title">{p.title}</div>
            {p.summary ? <div class="re-summary">{p.summary}</div> : null}
          </a>
        ))}
      </div>
    ) : (
      <div class="re-empty">
        暂无内容（请先在 Contentful 创建内容并发布，或检查 content_type / 字段名是否一致）。
      </div>
    )}
  </section>

  <style>
    .re-block{ margin: 22px 0 34px; }
    .re-h3{ font-weight: 800; margin: 0 0 14px; }
    .re-head-row{ display:flex; align-items:center; justify-content:space-between; gap:12px; margin-bottom:14px; }

    .re-more{ text-decoration:none; font-weight:700; opacity:.75; }
    .re-more:hover{ opacity:1; }

    .re-choice-list{
      display:flex;
      flex-wrap:wrap;
      gap:12px;
    }
    .re-choice{
      display:inline-block;
      padding:10px 14px;
      border:1px solid rgba(0,0,0,.12);
      border-radius:999px;
      text-decoration:none;
      font-weight:700;
      background:#fff;
    }

    .re-post-list{
      display:grid;
      grid-template-columns: repeat(2, minmax(0, 1fr));
      gap: 14px;
    }
    @media (max-width: 860px){
      .re-post-list{ grid-template-columns: 1fr; }
    }
    .re-post-card{
      display:block;
      padding:16px;
      border:1px solid rgba(0,0,0,.12);
      border-radius:16px;
      text-decoration:none;
      background:#fff;
    }
    .re-post-top{ display:flex; justify-content:space-between; gap:10px; margin-bottom:10px; }
    .re-tag{
      display:inline-flex;
      align-items:center;
      padding:6px 10px;
      border-radius:999px;
      background: rgba(11,31,107,.08);
      font-weight:800;
    }
    .re-date{ opacity:.7; font-size:12px; margin-top:4px; }
    .re-title{ font-weight:900; font-size:16px; margin-bottom:8px; }
    .re-summary{ opacity:.85; line-height:1.8; }
    .re-empty{ padding:14px; border:1px dashed rgba(0,0,0,.18); border-radius:16px; opacity:.8; background:#fff; }
  </style>
</TemplateLayout>